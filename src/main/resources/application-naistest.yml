micronaut:
  security:
    token:
      jwt:
        claims:
          values:
            allowed-audiences:
              - dapla-cli
              - onyxia-api
  http:
    services:
      dapla-team-api:
        url: http://dapla-team-api.dapla-platform

mongodb:
  uri: ${MONGODB_CONNECTION_STRING:}
