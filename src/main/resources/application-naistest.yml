micronaut:
  security:
    token:
      jwt:
        claims:
          values:
            allowed-audiences:
              - dapla-cli
              - onyxia-api

mongodb:
  uri: ${MONGODB_CONNECTION_STRING:}
