micronaut:
  security:
    token:
      jwt:
        claims:
          values:
            allowed-audiences:
              - dapla-cli
              - onyxia-api
              - dapla-team-api

mongodb:
  uri: ${MONGODB_CONNECTION_STRING:}
