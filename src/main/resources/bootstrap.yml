# Enable distributed configuration clients
micronaut:
  config-client:
    enabled: true
# Enable Kubernetes secret mounting as configuration properties
kubernetes:
  client:
    secrets:
      enabled: true
      labels:
        app.kubernetes.io/component: mongodb
